"""Tests for the tiny_skia_cpp port."""

package(default_visibility = ["//visibility:public"])

load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "tiny_skia_core_tests",
    srcs = ["TinySkiaPortingTest.cpp"],
    copts = ["-std=c++20"],
    deps = ["//src:tiny_skia_lib"],
)
